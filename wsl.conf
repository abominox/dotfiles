# WSL2 configuration
# Deploy to /etc/wsl.conf on WSL2 instances

[interop]
# Prevent Windows PATH from being injected into Linux PATH
appendWindowsPath = false

[boot]
# Enable systemd support
systemd = true
